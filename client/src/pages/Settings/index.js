import React from 'react'

import { Banner } from '../../components/UI'

const Settings = () => {
  return (
    <div>
      <Banner title='Settings' />
    </div>
  )
}

export default Settings
